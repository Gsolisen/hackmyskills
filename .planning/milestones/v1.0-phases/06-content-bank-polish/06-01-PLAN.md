---
phase: 06-content-bank-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hms/content/kubernetes.yaml
  - src/hms/content/terraform.yaml
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "kubernetes.yaml contains at least 30 L1, 10 L2, and 5 L3 questions"
    - "terraform.yaml contains at least 30 L1, 10 L2, and 5 L3 questions"
    - "Every question in both files has version_tag and last_verified fields"
    - "All questions pass schema validation via hms validate-content"
    - "No duplicate questions detected across content files"
  artifacts:
    - path: "src/hms/content/kubernetes.yaml"
      provides: "Curated Kubernetes question bank with 45+ cards across L1/L2/L3"
      contains: "tier: L3"
    - path: "src/hms/content/terraform.yaml"
      provides: "Curated Terraform question bank with 45+ cards across L1/L2/L3"
      contains: "tier: L3"
  key_links:
    - from: "src/hms/content/kubernetes.yaml"
      to: "src/hms/loader.py"
      via: "YAML schema validation at load time"
      pattern: "validate_question"
    - from: "src/hms/content/terraform.yaml"
      to: "src/hms/loader.py"
      via: "YAML schema validation at load time"
      pattern: "validate_question"
---

<objective>
Expand the existing kubernetes.yaml (6 cards) and terraform.yaml (6 cards) to meet the Phase 6 content minimums: 30+ L1, 10+ L2, 5+ L3 per topic.

Purpose: CONT-01 through CONT-04 require a curated question bank with minimum card counts per tier, all tagged with version_tag and last_verified.
Output: Two expanded YAML files totaling 90+ curated DevOps questions.
</objective>

<execution_context>
@C:/Users/sirka/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sirka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-bank-polish/06-CONTEXT.md
@.planning/phases/06-content-bank-polish/06-RESEARCH.md

<interfaces>
<!-- Key types and schema the executor must follow. Extracted from loader.py. -->

From src/hms/loader.py:
```python
REQUIRED_BASE_FIELDS = {"id", "type", "topic", "tier", "tags", "version_tag", "last_verified"}
VALID_TYPES = {"flashcard", "scenario", "command-fill", "explain-concept"}
VALID_TIERS = {"L1", "L2", "L3"}
VALID_SOURCES = {"curated", "ai"}

TYPE_REQUIRED_FIELDS = {
    "flashcard": {"front", "back"},
    "command-fill": {"prompt", "command", "accept_partial"},
    "scenario": {"situation", "question", "answer", "explanation"},
    "explain-concept": {"concept", "model_answer"},
}
```

From src/hms/content/kubernetes.yaml (reference format):
```yaml
questions:
  - id: k8s-pod-lifecycle-001
    type: flashcard
    topic: kubernetes
    tier: L1
    tags: [pods, lifecycle]
    version_tag: k8s-1.29
    last_verified: "2026-01-01"
    front: "What are the five phases of a Kubernetes Pod lifecycle?"
    back: "Pending -> Running -> Succeeded -> Failed -> Unknown"
```

ID naming convention:
- kubernetes: k8s-{subtopic}-{number} (e.g., k8s-svc-types-001)
- terraform: tf-{subtopic}-{number} (e.g., tf-state-backend-001)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand kubernetes.yaml to 45+ curated questions</name>
  <files>src/hms/content/kubernetes.yaml</files>
  <read_first>
    - src/hms/content/kubernetes.yaml (current 6 questions to preserve)
    - src/hms/loader.py (REQUIRED_BASE_FIELDS, TYPE_REQUIRED_FIELDS, VALID_TYPES, VALID_TIERS for schema compliance)
    - .planning/phases/06-content-bank-polish/06-RESEARCH.md (subtopic distribution and question type guidelines)
  </read_first>
  <action>
Expand kubernetes.yaml from 6 questions to 45+ questions. Preserve all 6 existing questions verbatim. Add new questions following these specifications:

**L1 (recall) cards -- add 26+ new cards (target: 30 total L1):**
Mix of flashcard (60%) and command-fill (40%). Subtopic distribution:
- Pods/Containers: 8 L1 total (5 new flashcard + 3 new)
- Deployments/ReplicaSets: 5 L1 (flashcard + command-fill mix)
- Services/Networking: 5 L1 (ClusterIP, NodePort, LoadBalancer, Ingress concepts)
- ConfigMaps/Secrets: 4 L1 (creation, usage, mounting)
- Storage/PV/PVC: 4 L1 (volume types, PV/PVC lifecycle, StorageClass)
- RBAC/Security: 4 L1 (Role, ClusterRole, RoleBinding, ServiceAccount)

**L2 (application) cards -- add 8+ new cards (target: 10 total L2):**
Mix of scenario (50%), command-fill (30%), explain-concept (20%). Based on real-world production incidents:
- Pods: 2 L2 (OOMKilled diagnosis, liveness probe failure debugging)
- Deployments: 2 L2 (rolling update stuck, rollback scenario)
- Services: 2 L2 (service selector mismatch diagnosis, Ingress 502 troubleshooting)
- ConfigMaps/Secrets: 1 L2 (secret rotation scenario)
- RBAC: 2 L2 (RBAC misconfiguration, ServiceAccount token troubleshooting)

**L3 (scenario) cards -- add 5 new cards (target: 5 total L3):**
Primarily scenario type (80%) with explain-concept (20%). Production incident style:
- k8s-oom-incident-L3-001: Pod OOMKilled with "here are the logs" diagnosis
- k8s-deploy-rollback-L3-001: Deployment rollout stuck, describe output provided
- k8s-network-policy-L3-001: Pod can't reach service, network policy misconfiguration
- k8s-pv-reclaim-L3-001: PVC stuck in Terminating, storage reclaim scenario
- k8s-rbac-escalation-L3-001: explain-concept on RBAC privilege escalation prevention

Every question MUST have these fields:
- id: k8s-{subtopic}-{number} format, unique across the file
- type: one of flashcard, scenario, command-fill, explain-concept
- topic: kubernetes
- tier: L1, L2, or L3
- tags: list of relevant subtopic tags
- version_tag: k8s-1.29
- last_verified: "2026-01-01"
- Type-specific fields per TYPE_REQUIRED_FIELDS

Question quality guidelines:
- L1 flashcard: clear, concise question on one concept; answer is 1-3 sentences
- L1 command-fill: realistic kubectl command scenarios
- L2 scenario: provide specific symptoms/logs, ask for diagnostic steps
- L3 scenario: realistic incident with multi-step resolution, include log excerpts or kubectl output in the situation field
- No overlapping questions across subtopics that would trigger >70% Jaccard similarity
  </action>
  <verify>
    <automated>cd C:/Users/sirka/OneDrive/Documentos/repos/hackmyskills && pip install -e . -q 2>/dev/null && python -m hms validate-content</automated>
  </verify>
  <acceptance_criteria>
    - kubernetes.yaml contains at least 30 questions with `tier: L1`
    - kubernetes.yaml contains at least 10 questions with `tier: L2`
    - kubernetes.yaml contains at least 5 questions with `tier: L3`
    - Every question contains `version_tag: k8s-1.29`
    - Every question contains `last_verified: "2026-01-01"`
    - Every question has `topic: kubernetes`
    - All 6 original questions (k8s-pod-lifecycle-001, k8s-pod-restart-policy-001, k8s-deployment-scale-001, k8s-kubectl-get-pods-001, k8s-crashloop-scenario-001, k8s-etcd-concept-001) are still present
    - `python -m hms validate-content` exits with code 0
    - No duplicate warnings in validate-content output
  </acceptance_criteria>
  <done>kubernetes.yaml has 45+ questions (30+ L1, 10+ L2, 5+ L3), all pass schema validation, no duplicates detected</done>
</task>

<task type="auto">
  <name>Task 2: Expand terraform.yaml to 45+ curated questions</name>
  <files>src/hms/content/terraform.yaml</files>
  <read_first>
    - src/hms/content/terraform.yaml (current 6 questions to preserve)
    - src/hms/loader.py (schema fields)
    - src/hms/content/kubernetes.yaml (as written by Task 1, to avoid cross-file duplicates)
    - .planning/phases/06-content-bank-polish/06-RESEARCH.md (subtopic distribution)
  </read_first>
  <action>
Expand terraform.yaml from 6 questions to 45+ questions. Preserve all 6 existing questions verbatim. Add new questions following these specifications:

**L1 (recall) cards -- add 25+ new cards (target: 30 total L1):**
Mix of flashcard (60%) and command-fill (40%). Subtopic distribution:
- Core workflow (init/plan/apply): 6 L1 total (3 new, keeping 3 existing)
- State management: 5 L1 (remote state, locking, terraform state commands)
- Variables/Outputs: 5 L1 (variable types, defaults, locals, outputs, sensitive)
- Modules: 5 L1 (module sources, version constraints, passing variables)
- Providers/Resources: 5 L1 (provider config, resource lifecycle, depends_on, count/for_each)
- Backends/Workspaces: 4 L1 (S3 backend, workspace commands, state isolation)

**L2 (application) cards -- add 9+ new cards (target: 10 total L2):**
Mix of scenario (50%), command-fill (30%), explain-concept (20%):
- Core workflow: 2 L2 (plan output interpretation, targeted apply)
- State management: 2 L2 (state mv scenario, state import for existing resource)
- Variables/Outputs: 2 L2 (complex variable types, dynamic blocks)
- Modules: 2 L2 (module versioning conflict, module output chaining)
- Backends: 1 L2 (backend migration scenario)

**L3 (scenario) cards -- add 5 new cards (target: 5 total L3):**
Production incident style:
- tf-state-lock-L3-001: State file locked, team member crashed mid-apply, resolution steps
- tf-module-drift-L3-001: Module upgrade breaks downstream resources, rollback strategy
- tf-import-legacy-L3-001: Importing existing AWS infrastructure into Terraform management
- tf-workspace-migration-L3-001: Migrating from single workspace to multi-workspace
- tf-provider-upgrade-L3-001: explain-concept on provider upgrade strategies and pinning

Every question MUST have:
- id: tf-{subtopic}-{number} format
- type: one of flashcard, scenario, command-fill, explain-concept
- topic: terraform
- tier: L1, L2, or L3
- tags: list of relevant tags
- version_tag: tf-1.7
- last_verified: "2026-01-01"
- Type-specific required fields

Use distinct vocabulary from kubernetes.yaml to avoid cross-file >70% Jaccard overlap.
  </action>
  <verify>
    <automated>cd C:/Users/sirka/OneDrive/Documentos/repos/hackmyskills && python -m hms validate-content</automated>
  </verify>
  <acceptance_criteria>
    - terraform.yaml contains at least 30 questions with `tier: L1`
    - terraform.yaml contains at least 10 questions with `tier: L2`
    - terraform.yaml contains at least 5 questions with `tier: L3`
    - Every question contains `version_tag: tf-1.7`
    - Every question contains `last_verified: "2026-01-01"`
    - Every question has `topic: terraform`
    - All 6 original questions (tf-init-001, tf-plan-apply-001, tf-state-001, tf-destroy-cmd-001, tf-drift-scenario-001, tf-provider-concept-001) are still present
    - `python -m hms validate-content` exits with code 0
    - No duplicate warnings in validate-content output
  </acceptance_criteria>
  <done>terraform.yaml has 45+ questions (30+ L1, 10+ L2, 5+ L3), all pass schema validation, no duplicates detected</done>
</task>

</tasks>

<verification>
After both tasks:
1. `python -m hms validate-content` exits 0 with no errors or duplicates
2. Combined kubernetes.yaml + terraform.yaml total is 90+ questions
3. Each file independently has 30+ L1, 10+ L2, 5+ L3
</verification>

<success_criteria>
- kubernetes.yaml expanded from 6 to 45+ questions across all three tiers
- terraform.yaml expanded from 6 to 45+ questions across all three tiers
- All questions conform to the existing YAML schema (REQUIRED_BASE_FIELDS + TYPE_REQUIRED_FIELDS)
- All questions have version_tag and last_verified fields (CONT-04)
- No cross-file or intra-file duplicate detection triggers
- `hms validate-content` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-bank-polish/06-01-SUMMARY.md`
</output>
