---
phase: 06-content-bank-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hms/content/cicd.yaml
  - src/hms/content/bash.yaml
  - src/hms/content/aws.yaml
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "cicd.yaml contains at least 30 L1, 10 L2, and 5 L3 questions"
    - "bash.yaml contains at least 30 L1, 10 L2, and 5 L3 questions"
    - "aws.yaml contains at least 30 L1, 10 L2, and 5 L3 questions"
    - "Every question in all three files has version_tag and last_verified fields"
    - "All questions pass schema validation via hms validate-content"
    - "No duplicate questions detected across any content files"
  artifacts:
    - path: "src/hms/content/cicd.yaml"
      provides: "Curated CI/CD question bank with 45+ cards emphasizing GitHub Actions"
      contains: "topic: cicd"
    - path: "src/hms/content/bash.yaml"
      provides: "Curated Bash question bank with 45+ cards"
      contains: "topic: bash"
    - path: "src/hms/content/aws.yaml"
      provides: "Curated AWS question bank with 45+ cards covering core and DevOps services"
      contains: "topic: aws"
  key_links:
    - from: "src/hms/content/cicd.yaml"
      to: "src/hms/loader.py"
      via: "YAML schema validation at load time"
      pattern: "validate_question"
    - from: "src/hms/content/bash.yaml"
      to: "src/hms/loader.py"
      via: "YAML schema validation at load time"
      pattern: "validate_question"
    - from: "src/hms/content/aws.yaml"
      to: "src/hms/loader.py"
      via: "YAML schema validation at load time"
      pattern: "validate_question"
---

<objective>
Create three new content files (cicd.yaml, bash.yaml, aws.yaml) each with 45+ curated DevOps questions across L1/L2/L3 tiers.

Purpose: CONT-01 through CONT-04 require a curated question bank covering 5 topics. This plan creates the 3 topics that don't yet exist.
Output: Three new YAML files totaling 135+ curated DevOps questions.
</objective>

<execution_context>
@C:/Users/sirka/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sirka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-bank-polish/06-CONTEXT.md
@.planning/phases/06-content-bank-polish/06-RESEARCH.md

<interfaces>
<!-- Key types and schema the executor must follow. Extracted from loader.py. -->

From src/hms/loader.py:
```python
REQUIRED_BASE_FIELDS = {"id", "type", "topic", "tier", "tags", "version_tag", "last_verified"}
VALID_TYPES = {"flashcard", "scenario", "command-fill", "explain-concept"}
VALID_TIERS = {"L1", "L2", "L3"}
VALID_SOURCES = {"curated", "ai"}

TYPE_REQUIRED_FIELDS = {
    "flashcard": {"front", "back"},
    "command-fill": {"prompt", "command", "accept_partial"},
    "scenario": {"situation", "question", "answer", "explanation"},
    "explain-concept": {"concept", "model_answer"},
}
```

From src/hms/content/kubernetes.yaml (reference YAML format):
```yaml
questions:
  - id: k8s-pod-lifecycle-001
    type: flashcard
    topic: kubernetes
    tier: L1
    tags: [pods, lifecycle]
    version_tag: k8s-1.29
    last_verified: "2026-01-01"
    front: "What are the five phases of a Kubernetes Pod lifecycle?"
    back: "Pending -> Running -> Succeeded -> Failed -> Unknown"
```

ID naming convention:
- cicd: cicd-{subtopic}-{number} (e.g., cicd-gh-actions-workflow-001)
- bash: bash-{subtopic}-{number} (e.g., bash-redirect-001)
- aws: aws-{subtopic}-{number} (e.g., aws-iam-policy-001)

Version tags:
- cicd: gh-actions-v4
- bash: bash-5.2
- aws: aws-2024

All questions use last_verified: "2026-01-01"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cicd.yaml with 45+ curated CI/CD questions</name>
  <files>src/hms/content/cicd.yaml</files>
  <read_first>
    - src/hms/loader.py (REQUIRED_BASE_FIELDS, TYPE_REQUIRED_FIELDS for schema compliance)
    - src/hms/content/kubernetes.yaml (reference YAML format and quality standard)
    - .planning/phases/06-content-bank-polish/06-CONTEXT.md (CI/CD should emphasize GitHub Actions per user decision)
    - .planning/phases/06-content-bank-polish/06-RESEARCH.md (subtopic distribution)
  </read_first>
  <action>
Create a new file src/hms/content/cicd.yaml with 45+ curated CI/CD questions. Per user decision: emphasize GitHub Actions as the primary CI/CD tool example, with general CI/CD concepts mixed in.

**L1 (recall) cards -- 30+ cards:**
Mix of flashcard (60%) and command-fill (40%). Subtopic distribution:
- GitHub Actions basics: 8 L1 (workflow triggers, jobs, steps, runners, actions marketplace, YAML syntax, environment variables, secrets)
- Workflow syntax: 6 L1 (on: events, matrix strategy, needs keyword, if conditionals, timeout-minutes, concurrency)
- CI concepts (pipelines, stages): 6 L1 (continuous integration vs delivery vs deployment, pipeline stages, build artifacts, test strategies, branch protection, merge checks)
- Artifacts/Caching: 5 L1 (actions/cache, actions/upload-artifact, dependency caching, cache keys, artifact retention)
- Deployment strategies: 5 L1 (blue-green, canary, rolling update, feature flags, environment approvals)

**L2 (application) cards -- 10+ cards:**
Mix of scenario (50%), command-fill (30%), explain-concept (20%):
- GitHub Actions: 2 L2 (workflow debugging with act, reusable workflow patterns)
- Workflow syntax: 2 L2 (matrix build failure diagnosis, conditional job execution)
- CI concepts: 2 L2 (flaky test handling strategy, pipeline optimization for speed)
- Artifacts/Caching: 2 L2 (cache invalidation scenario, artifact size management)
- Deployment: 2 L2 (rollback strategy after failed deployment, environment promotion gates)

**L3 (scenario) cards -- 5+ cards:**
Production incident style with realistic CI/CD failure scenarios:
- cicd-pipeline-timeout-L3-001: CI pipeline timing out on large monorepo, optimization strategy
- cicd-secret-leak-L3-001: Secret accidentally exposed in CI logs, incident response
- cicd-deploy-rollback-L3-001: Production deployment failed mid-rollout, steps to recover
- cicd-matrix-debug-L3-001: Matrix build failing on specific OS/version combo, diagnosis approach
- cicd-cache-corruption-L3-001: explain-concept on CI cache invalidation strategies

Every question MUST have:
- id: cicd-{subtopic}-{number} format, unique across all content files
- type: one of flashcard, scenario, command-fill, explain-concept
- topic: cicd
- tier: L1, L2, or L3
- tags: list of relevant tags
- version_tag: gh-actions-v4
- last_verified: "2026-01-01"
- Type-specific required fields per TYPE_REQUIRED_FIELDS

The file MUST start with `questions:` as the root key (matching existing YAML format).
  </action>
  <verify>
    <automated>cd C:/Users/sirka/OneDrive/Documentos/repos/hackmyskills && pip install -e . -q 2>/dev/null && python -c "from hms.loader import load_questions; qs = load_questions('src/hms/content/cicd.yaml'); print(f'Loaded {len(qs)} questions'); l1 = sum(1 for q in qs if q['tier']=='L1'); l2 = sum(1 for q in qs if q['tier']=='L2'); l3 = sum(1 for q in qs if q['tier']=='L3'); print(f'L1={l1} L2={l2} L3={l3}'); assert l1>=30 and l2>=10 and l3>=5, f'Count check failed: L1={l1} L2={l2} L3={l3}'"</automated>
  </verify>
  <acceptance_criteria>
    - src/hms/content/cicd.yaml exists and starts with `questions:`
    - cicd.yaml contains at least 30 questions with `tier: L1`
    - cicd.yaml contains at least 10 questions with `tier: L2`
    - cicd.yaml contains at least 5 questions with `tier: L3`
    - Every question contains `version_tag: gh-actions-v4`
    - Every question contains `last_verified: "2026-01-01"`
    - Every question has `topic: cicd`
    - Every question ID starts with `cicd-`
    - load_questions('src/hms/content/cicd.yaml') succeeds without ValueError
  </acceptance_criteria>
  <done>cicd.yaml has 45+ questions (30+ L1, 10+ L2, 5+ L3), all pass schema validation</done>
</task>

<task type="auto">
  <name>Task 2: Create bash.yaml and aws.yaml with 45+ curated questions each</name>
  <files>src/hms/content/bash.yaml, src/hms/content/aws.yaml</files>
  <read_first>
    - src/hms/loader.py (schema fields)
    - src/hms/content/kubernetes.yaml (reference format)
    - src/hms/content/cicd.yaml (as written by Task 1, to avoid cross-file duplicates)
    - .planning/phases/06-content-bank-polish/06-CONTEXT.md (AWS scope decision)
    - .planning/phases/06-content-bank-polish/06-RESEARCH.md (subtopic distributions)
  </read_first>
  <action>
Create two new YAML files: bash.yaml and aws.yaml, each with 45+ curated questions.

## bash.yaml

**L1 (recall) cards -- 30+ cards:**
Mix of flashcard (60%) and command-fill (40%). Subtopics:
- Commands/Builtins: 8 L1 (cd, ls, cp, mv, rm, chmod, chown, find, which, type, alias)
- Redirection/Pipes: 5 L1 (stdin/stdout/stderr, |, >, >>, 2>&1, /dev/null, tee)
- Text processing: 6 L1 (grep, sed, awk, cut, sort, uniq, wc, tr, head, tail)
- Scripting: 6 L1 (variables, if/else, for/while loops, functions, exit codes, $?, set -e)
- Process management: 5 L1 (ps, top, kill, bg, fg, nohup, &, jobs, signals)

**L2 (application) cards -- 10+ cards:**
Mix of scenario (50%), command-fill (30%), explain-concept (20%):
- Commands: 2 L2 (find with exec, xargs pipeline construction)
- Redirection: 2 L2 (complex pipeline debugging, process substitution)
- Text processing: 2 L2 (awk multi-column processing, sed in-place with backup)
- Scripting: 2 L2 (error handling patterns, signal trapping)
- Process management: 2 L2 (zombie process diagnosis, resource monitoring)

**L3 (scenario) cards -- 5+ cards:**
Production incident style:
- bash-log-analysis-L3-001: Parse production logs to find error patterns using grep/awk pipeline
- bash-disk-full-L3-001: Disk full emergency, find and clean large files
- bash-script-debug-L3-001: Shell script failing silently, debugging approach with set -x
- bash-permission-L3-001: Permission denied errors in deployment script, diagnosis
- bash-proc-leak-L3-001: explain-concept on identifying and cleaning up zombie processes

Every question MUST have: id with bash-{subtopic}-{number} format, topic: bash, version_tag: bash-5.2, last_verified: "2026-01-01", all required fields.

## aws.yaml

**L1 (recall) cards -- 30+ cards:**
Mix of flashcard (60%) and command-fill (40%). Per user decision, AWS scope covers BOTH core services AND DevOps-specific services. Subtopics:
- IAM (users, roles, policies): 6 L1 (users vs roles, policy types, trust policies, MFA, access keys, STS)
- EC2/VPC: 5 L1 (instance types, security groups, subnets, route tables, NAT gateway, elastic IP)
- S3: 5 L1 (bucket policies, storage classes, versioning, lifecycle rules, encryption)
- ECS/ECR: 5 L1 (task definitions, services, Fargate vs EC2 launch, ECR image push, container definitions)
- CloudFormation/CloudWatch: 5 L1 (stacks, templates, change sets, alarms, log groups, metrics)
- Route53/ELB: 4 L1 (record types, hosted zones, ALB vs NLB, target groups, health checks)

**L2 (application) cards -- 10+ cards:**
Mix of scenario (50%), command-fill (30%), explain-concept (20%):
- IAM: 2 L2 (least privilege policy design, cross-account role assumption)
- EC2/VPC: 2 L2 (security group rule debugging, VPC peering setup)
- S3: 2 L2 (bucket policy vs IAM policy conflict, cross-region replication)
- ECS: 1 L2 (task definition memory/CPU tuning)
- CloudFormation: 2 L2 (stack rollback diagnosis, drift detection)
- Route53/ELB: 1 L2 (health check failure troubleshooting)

**L3 (scenario) cards -- 5+ cards:**
Production incident style:
- aws-iam-breach-L3-001: Compromised access key incident response steps
- aws-ec2-unreachable-L3-001: EC2 instance unreachable, security group + route table + NACL diagnosis
- aws-s3-public-L3-001: S3 bucket accidentally public, remediation steps
- aws-ecs-oom-L3-001: ECS task OOMKilled, task definition tuning and CloudWatch analysis
- aws-cf-rollback-L3-001: explain-concept on CloudFormation rollback triggers and strategies

Every question MUST have: id with aws-{subtopic}-{number} format, topic: aws, version_tag: aws-2024, last_verified: "2026-01-01", all required fields.

**CRITICAL:** Use distinct vocabulary across all 5 content files. Avoid generic phrasing like "What command..." that could trigger >70% Jaccard overlap with questions in other topic files. Use tool-specific terminology (kubectl, terraform, gh, bash, aws-cli).
  </action>
  <verify>
    <automated>cd C:/Users/sirka/OneDrive/Documentos/repos/hackmyskills && python -c "
from hms.loader import load_questions
for f in ['bash', 'aws']:
    qs = load_questions(f'src/hms/content/{f}.yaml')
    l1 = sum(1 for q in qs if q['tier']=='L1')
    l2 = sum(1 for q in qs if q['tier']=='L2')
    l3 = sum(1 for q in qs if q['tier']=='L3')
    print(f'{f}: {len(qs)} total, L1={l1} L2={l2} L3={l3}')
    assert l1>=30 and l2>=10 and l3>=5, f'{f} count check failed'
print('All counts pass')
" && python -m hms validate-content</automated>
  </verify>
  <acceptance_criteria>
    - src/hms/content/bash.yaml exists, starts with `questions:`, has 45+ questions
    - src/hms/content/aws.yaml exists, starts with `questions:`, has 45+ questions
    - bash.yaml has at least 30 L1, 10 L2, 5 L3 questions
    - aws.yaml has at least 30 L1, 10 L2, 5 L3 questions
    - bash.yaml questions all have `topic: bash` and `version_tag: bash-5.2`
    - aws.yaml questions all have `topic: aws` and `version_tag: aws-2024`
    - All questions have `last_verified: "2026-01-01"`
    - bash.yaml question IDs all start with `bash-`
    - aws.yaml question IDs all start with `aws-`
    - `python -m hms validate-content` exits 0 across ALL 5 content files with no duplicates
  </acceptance_criteria>
  <done>bash.yaml and aws.yaml each have 45+ questions (30+ L1, 10+ L2, 5+ L3), all pass validation, no cross-file duplicates across all 5 topic files</done>
</task>

</tasks>

<verification>
After both tasks:
1. `python -m hms validate-content` exits 0 with no errors or duplicates across all 5 files
2. Each of cicd.yaml, bash.yaml, aws.yaml independently has 30+ L1, 10+ L2, 5+ L3
3. Total new content: 135+ questions across 3 new files
</verification>

<success_criteria>
- Three new YAML files created: cicd.yaml, bash.yaml, aws.yaml
- Each file has 45+ questions meeting the 30/10/5 tier minimums
- CI/CD content emphasizes GitHub Actions per user decision
- AWS content covers both core and DevOps-specific services per user decision
- All questions conform to YAML schema (REQUIRED_BASE_FIELDS + TYPE_REQUIRED_FIELDS)
- All questions have version_tag and last_verified (CONT-04)
- No cross-file or intra-file duplicate detection triggers
- `hms validate-content` passes cleanly across all 5 topic files
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-bank-polish/06-02-SUMMARY.md`
</output>
